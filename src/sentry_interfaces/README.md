# sentry_interfaces

统一的哨兵系统消息和服务接口包。消息从原工程的 `pb_rm_interfaces` 和
`rmoss_interfaces` 汇总，后续新节点优先依赖本包，避免跨多个历史接口包。

当前包含：

- 云台、底盘、射击和统一控制命令
- 裁判系统状态、血量、事件和 RFID 数据
- 模式、任务、颜色和相机信息服务

接口定义应保持稳定；协议变更时优先新增字段或新消息，避免破坏已有节点。
